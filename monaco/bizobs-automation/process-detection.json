{
  "enabled": true,
  "value": {
    "enabled": true,
    "name": "{{ .name }}",
    "description": "Detect and group BizObs processes by service name for proper entity mapping",
    "processGroupIdentifier": "{{.Env.DT_SERVICE_NAME}}",
    "processGroupNaming": "{{.Env.DT_SERVICE_NAME}}",
    "conditions": [
      {
        "attribute": "PROCESS_ENVIRONMENT_VARIABLE",
        "comparisonType": "EXISTS",
        "key": "DT_SERVICE_NAME"
      }
    ]
  }
}
